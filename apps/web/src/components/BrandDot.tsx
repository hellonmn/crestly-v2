/**
 * The orange period that ends every Crestly page title and most card titles.
 * "Students<BrandDot />" → renders "Students" + an orange period.
 */
export function BrandDot() {
  return <span className="brand-dot">.</span>;
}
