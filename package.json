{
  "name": "crestly",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.10.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build:shared": "npm run build -w @crestly/shared",
    "predev": "npm run build:shared",
    "dev": "concurrently -n shared,api,web -c yellow,blue,magenta \"npm run dev -w @crestly/shared\" \"npm run dev -w @crestly/api\" \"npm run dev -w @crestly/web\"",
    "dev:api": "npm run build:shared && npm run dev -w @crestly/api",
    "dev:web": "npm run build:shared && npm run dev -w @crestly/web",
    "build": "npm run build:shared && npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "db:pull": "npm run db:pull -w @crestly/api",
    "db:generate": "npm run db:generate -w @crestly/api",
    "db:studio": "npm run db:studio -w @crestly/api"
  },
  "devDependencies": {
    "concurrently": "9.0.1",
    "typescript": "5.6.3"
  }
}
